{
  "name": "pota-2-qrz",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "check-lines": "bash scripts/check-lines.sh",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
